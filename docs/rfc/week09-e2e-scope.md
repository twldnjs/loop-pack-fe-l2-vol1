# RFC — 9주차 인증과 E2E 범위

- 작성 시작: 2026-09-04
- 브랜치: `feat/week-09` (베이스 `39114daa` = 8주차 tip + 9주차 스타터)
- 원칙: **E2E는 붙일 근거를 먼저 만들고, 그 근거대로만 만든다.** 이 문서는 구현보다 먼저 커밋한다 — 절마다 "정한 시점"이 커밋 시각으로 남도록, 절이 채워질 때마다 따로 커밋한다.

> 8주차 리뷰에서 "커밋 9개가 24분 안에 올라가 '정하고 썼다'와 '쓰고 나눴다'를 구별할 수 없다"는 지적을 받았다. 이번 주는 결정 → (시간) → 구현 순서가 히스토리에 보이게 한다.

---

## 0. 1단계 결정 — 인증 (구현 전에 정한 것)

과제 문서는 1단계 결정을 PR 본문에 쓰라고 한다. PR 본문은 이 절을 가리킨다. 여기 먼저 적는 이유는 위와 같다 — 구현 커밋보다 앞선 시각이 필요하다.

### D1. 세션 상태는 서버 상태다 → TanStack Query

5주차부터 써온 클라이언트 상태(zustand)를 그대로 쓰지 않는다. 세션은 서버가 소유하고 요청마다 값이 달라질 수 있다(만료·로그아웃). "서버에서 오는 데이터 → 서버 상태"라는 상태 분류 기준 그대로다.

- `/api/auth/me`를 `authQueries.me()`로 조회한다.
- **초기 HTML 조건**: 서버(RSC)에서 `readSessionToken(cookie)`로 사용자를 읽어 `HydrationBoundary`로 내려준다. JS 실행 전 헤더에 로그인 상태가 있어야 하므로 클라이언트 fetch로만 채우면 조건을 못 지킨다.
- 쿠키를 읽는 위치는 `(commerce)` 레이아웃. 이 트리는 이미 동적이다(홈 `force-dynamic`, `/products`는 searchParams) — build 출력의 ○/ƒ 표가 바뀌지 않는지 구현 후 확인해 여기에 적는다.
  - **구현 후 기록(2026-09-04)**: `pnpm build` Route 표에서 ○(정적)는 구현 전과 같이 `/_not-found`·`/dialog-demo`·`/performance-lab/inp`·`/select-demo` 4개, 나머지는 전부 ƒ. `(commerce)` 아래에 정적이던 라우트가 없었으므로 `cookies()` 읽기로 정적 생성 범위가 줄지 않았다. 7주차 기준(LCP 측정 조건) 변동 없음.
  - **구현에서 배운 것**: 헤더의 보호 경로 링크(`/checkout`·`/orders`)는 `prefetch={false}`. 로그인 전에 prefetch하면 proxy의 리다이렉트 응답이 라우터 캐시에 남아, 로그인 후 `router.replace('/checkout')`가 캐시를 타고 다시 로그인 화면으로 간다(실브라우저 재현). 로그인 성공 시 `router.refresh()`도 같은 이유로 붙였다.

### D2. proxy는 쿠키 존재만 확인한다 (→ G절에서 정정: 서명까지 검증한다)

- `src/proxy.ts`(Next 16: `middleware.ts`는 deprecated). Edge 런타임이라 `node:crypto`를 쓰는 `auth.ts`를 import할 수 없다 → 서명 검증 불가. 쿠키 이름은 `auth-cookies.ts`에서만 가져온다.
- 존재만 확인하면 "쿠키는 있는데 만료된" 요청은 통과한다. 그 경우는 페이지·API의 실제 검증에서 401이 나고 D5로 흘러간다. 즉 **proxy = 미로그인 차단, D5 = 만료 처리**로 역할을 가른다. 이 구분이 "같은 401을 미로그인과 만료 중 무엇으로 볼지"의 기준이 된다.

### D3. 보호 경로 = 주문서 · 주문 내역만

| 경로 | 보호 | 이유 |
| --- | --- | --- |
| `/checkout` (주문서) | ✅ | 주문 API가 세션을 요구한다. 과제 필수 |
| `/orders` (주문 내역) | ✅ | 사용자별 데이터. 과제 필수 |
| 장바구니 · 위시리스트 | ❌ | 메모리 zustand의 익명 로컬 상태. 로그인 전에도 담을 수 있어야 하고, 서버에 없는 데이터를 로그인으로 가릴 이유가 없다 |
| 마이페이지 | 만들지 않음 | 헤더의 로그인 상태·로그아웃이 그 역할을 한다. 보호할 화면을 늘리면 E2E 후보만 늘어난다 |

주문서·주문 내역은 지금 없으므로 **만든다**. 주문 API가 `{productId, quantity}`를 받는데 현재 카트에는 quantity가 없다 → 주문서에서 수량 1로 보낸다(카트 모델 변경은 이번 주 범위 밖).

### D4. 복원 파라미터는 `next`, 검증은 "우리 경로처럼 보이는가"

- `/login?next=<원래 경로>`. proxy가 붙이고 로그인 성공 후 그 값으로 `router.replace`.
- 허용 조건: `/`로 시작 · `//`로 시작하지 않음 · 백슬래시 없음 · 스킴(`:`) 없음. 하나라도 어긋나면 `/`로. `new URL(next, origin).origin === origin` 같은 파싱 기반 검증은 `\\evil.com` 류를 브라우저마다 다르게 해석하므로 문자열 규칙으로 막는다.

### D5. 401은 한 곳에서 — api 레이어가 분류하고, `(commerce)` 레이아웃의 경계가 보여준다

- `shared/api`에 `UnauthorizedError`(401 전용)를 추가한다. `HttpError(401)`을 화면마다 `status === 401`로 검사하지 않는다.
- 만료 판정 기준: **proxy가 통과시킨 요청(쿠키 있음)에서 온 401 = 만료.** 쿠키가 없으면 proxy가 이미 로그인으로 보냈으므로 그 401은 화면까지 오지 않는다.
- 표시 위치: `(commerce)` 레이아웃 한 곳의 경계가 "세션이 만료됐어요 — 다시 로그인" + `/login?next=<현재 경로>` 링크를 보여준다. 화면마다 처리하지 않는다.
- 401은 **retry 0**. 재시도해도 같은 답이다(`query-client`의 retry 정책에 분기 추가).

### D6. 로그아웃해도 장바구니 · 위시리스트는 남긴다

**결정: 남긴다.**

- 카트는 세션의 소유물이 아니라 **브라우저 탭의 소유물**이다. 로그인 전에도 담을 수 있고(D3에서 보호하지 않기로 한 이유와 같다), 서버에도 계정에도 묶여 있지 않다. 소유권이 다른 상태를 로그아웃이 건드리면 상태 분류 기준("서버 상태 / 로컬 상태")을 코드가 스스로 어기는 것이다.
- 요즘 커머스의 일반적인 흐름과도 맞다 — **로그인 없이 담고, 주문하러 갈 때 로그인을 유도한다.** 이 앱도 그 구조다: 담기는 열려 있고 `/checkout`이 보호 경로다. 로그아웃이 카트를 지우면 "둘러보다 담고 → 로그인 → 마음 바꿔 로그아웃 → 계속 둘러보기"에서 담은 게 사라진다.
- 공용 기기 우려는 이미 "메모리 전용 · 새로고침이면 비워진다"는 현재 계약(8주차 E2E가 고정)이 맡고 있다. 로그아웃에 같은 역할을 중복시키지 않는다.
- 남기지 않은 것과의 경계: 로그아웃 `onSuccess`에서는 **세션에서 파생된 것만** 정리한다 — 세션 query 무효화 · analytics `reset()`. 카트·위시리스트 스토어는 건드리지 않는다. 코드 위치가 곧 이 구분을 보여준다.

기각한 선택지: 지운다(주문 주체가 바뀌는데 주문 재료가 남는다). 주문서는 진입 시점에 세션을 다시 확인하므로, 카트가 남아 있어도 **다른 계정으로 주문이 나가는 건 그 계정 사용자가 주문서에서 확인하고 누른 것**이다. 재료가 남는 것과 주문이 나가는 것은 다르다.

---

## A. 이벤트 스키마 (2단계 — 계측 코드보다 먼저)

### 이름 규칙 — 시드 로그 스키마를 그대로 쓴다

`snake_case`, `<대상>_<행위>` 순서(`product_list_view`, `cart_add`, `login_success`). 새 이름을 만들지 않는 이유: 이 스키마는 팀이 이미 합의해 30일치 로그가 쌓여 있는 것이고, 3단계에서 그 로그로 경로를 세운다. 이름이 같아야 "내 앱의 이 화면 = 로그의 이 이벤트"가 매핑 표 없이 성립한다.

| 계측 지점 (과제 지정) | 이벤트 | props | 내 앱에서 부르는 자리 |
| --- | --- | --- | --- |
| 목록 진입 | `product_list_view` | `category`·`sort`·`page` | `ProductsPage` 마운트·필터 변경 시(현재 URL 조건) |
| 상세 진입 | `product_detail_view` | — | **없음 — 상세 화면이 없다.** 계측하지 않고 이 사실을 기록한다 |
| 담기 | `cart_add` | `productId`·`quantity`(항상 1) | `AddToCartButton` 담기(빼기는 보내지 않음 — 시드에 `cart_remove`가 없다) |
| 로그인 시작 | `login_start` | `from`(= `next` 경로 또는 `direct`) | `/login` 진입 |
| 로그인 성공 | `login_success` | `from` | 로그인 mutation `onSuccess` (+ `identify(user.id)`) |
| 로그인 실패 | `login_fail` | `reason`(`INVALID_CREDENTIALS` \| `SERVER_ERROR`) | 로그인 mutation `onError` |
| 주문 시작 | `order_start` | `itemCount` | `/checkout` 진입(담은 상품이 있을 때) |
| 주문 완료 | `order_complete` | `orderId`·`itemCount` | 주문 mutation `onSuccess` |

시드에 있지만 이 앱에서 보내지 않는 것: `product_detail_view`(화면 없음), `wishlist_add`·`category_filter_change`·`sort_change`·`page_change`(과제 지정 지점 아님 — 목록 진입 이벤트의 props로 조건이 실린다), `client_error`(오류 계측은 이번 범위 밖).

**매핑 표**: 이름 차이 없음. props 차이 — 시드의 `order_start.productId`·`order_complete.totalPrice`는 보내지 않는다(카트에 수량·금액이 없고 주문 API 응답에 금액이 없다). 대신 `itemCount`·`orderId`를 보낸다. 집계 시 `order_*` 이벤트 **수**는 호환되고 금액 집계는 불가 — 이 차이를 표로 남기는 것이 매핑의 목적이다.

### 공통 프로퍼티 — `setCommonProperties()`

시드 로그의 최상위 필드와 같은 이름을 쓴다(집계 스크립트가 그대로 읽을 수 있게).

| 필드 | 값 | 이유 |
| --- | --- | --- |
| `sessionId` | 탭 단위 랜덤 id, `sessionStorage`에 보관 | 시드의 세션 정의("같은 세션의 이벤트는 같은 값")와 맞춘다. 탭을 닫으면 끝나는 것이 이 앱의 카트 수명과 같다 |
| `device` | 뷰포트 폭으로 `mobile`(<768) / `tablet`(<1024) / `desktop` | 시드의 세 값. UA 파싱보다 단순하고 결정적이다 |
| `ts` | `new Date().toISOString()` | 이벤트 발생 시점에 평가된다(`setCommonProperties`가 함수를 받는 이유) |

`userId`는 공통 프로퍼티가 아니라 `identify()`로 보낸다 — 로그인 전 이벤트에 붙지 않아야 시드와 같은 모양이 된다.

### 어디서 부르나

- `initAnalytics()` — 루트 레이아웃의 클라이언트 컴포넌트 `AnalyticsProvider` 1곳. 그 전에 발생한 `track()`은 로거의 큐가 잡는다. 이 위치면 첫 화면의 `product_list_view`가 큐 → 초기화 후 전송 순서로 나가는 것을 볼 수 있다.
- `identify(user.id)` — 로그인 mutation `onSuccess`. `reset()` — 로그아웃 mutation `onSuccess` (D6의 "세션에서 파생된 것만 정리"와 같은 자리).
- **컴포넌트가 `track()`을 직접 부르지 않는다.** `shared/analytics/events.ts`에 이벤트 이름·props를 타입으로 고정한 함수 한 겹(`trackCartAdd(productId)` 식)을 두고 features·pages의 model(훅·mutation 콜백)이 부른다. 이유: (1) 이름 오타·props 누락을 타입이 막는다 (2) 화면 코드에 문자열 리터럴이 흩어지지 않아 스키마를 바꿀 때 한 파일만 고친다 (3) `src/analytics/`(스타터)는 그대로 두고 shared가 그 위에 얹힌다 — FSD에서 shared는 도메인을 모르는데, 이벤트 이름은 도메인이다. 그래서 이벤트 함수는 `shared`가 아니라 **`entities/analytics`**… 가 아니라 각 슬라이스에 두는 게 맞지 않나 검토했다 → 이벤트 목록이 8개뿐이고 팀 스키마(외부 계약)라 한 파일이 낫다. `shared/analytics/events.ts`로 두고 "스키마 = 외부 계약"임을 주석으로 남긴다.

## B. 시드 로그 분석 (3단계)

재료는 `pnpm analyze:events`(`scripts/analyze-events.mts`)로 뽑았다. 스크립트는 노이즈 기준을 정하지 않는다 — 기본은 원본 집계이고, 아래 기준을 플래그로 넘겨 전/후를 나란히 냈다. 최종 실행:

```
pnpm analyze:events --drop-device-null --drop-dupes --fail-events login_fail,client_error
```

### 노이즈 기준 — 무엇을 걸렀고 왜

| 종류 | 기준 | 규모 | 근거 |
| --- | --- | --- | --- |
| 봇 | `device === null` | 486세션 / 486이벤트 | 이 486개는 **전부** 이벤트 1개짜리 `product_list_view`(all/latest/1 = 첫 화면 기본값)이고, **전부** UTC 1시에 시작하며, `ts`의 밀리초가 **전부** `.000`이다. 시작 시각 분포는 다른 23개 시각이 270~337인데 1시만 789 — 789−486=303으로 사람 세션 수는 다른 시각과 같다. 로그에 "봇" 표시는 없지만 네 신호(기기 없음·단일 이벤트·시각 집중·정각 타임스탬프)가 486개 전부에 동시에 겹친다. 1시 세션 전체를 빼는 대안은 사람 303명을 함께 버리므로 기각 |
| 중복 전송 | `sessionId·ts·name·props` 네 필드 완전 일치 | 77쌍 / 77세션 | 간격 0초의 동일 줄. 종류는 `product_list_view` 28 · `page_change` 23 · `category_filter_change` 21 · `sort_change` 5 — 전부 목록 조작이고 담기·로그인·주문에는 없다. ts만 다른 근접 중복(≤5초)은 **0**이라 기준을 넓힐 이유가 없다 |
| 오류 이벤트 | `client_error`는 **경로에서 빼지 않고 실패 이벤트로 센다** | 103건 / 103세션 | 전부 `IMAGE_LOAD_FAILED`, 직전 이벤트 100% `product_detail_view`. 빼면 그 세션의 마지막 이벤트가 상세로 바뀌어 상세 이탈률 A가 63.0→64.5%로 오르며 "왜 떠났는지"가 사라진다. 세션 자체를 빼는 것은 오류를 겪은 진짜 사용자를 지우는 것이라 기각. 13위(1.4%)라 순위에는 영향이 없다 |

### 걸러내기 전과 후

| | 원본 | 봇 제외 | 봇+중복 제외 |
| --- | --- | --- | --- |
| 세션 | 8000 | 7514 | 7514 |
| 이벤트 | 22427 | 21941 | 21864 |
| 순위(13개) | — | **전부 불변** | **전부 불변** |
| `product_list_view` 이탈률 A | 20.4% | 15.2% | 15.2% |
| 상세 비율 | 58.2% | 62.0% | 62.0% |

- **봇 제외가 순위를 안 바꾼 이유**: 제거된 486세션이 전부 1위 이벤트(`product_list_view`) **하나만** 갖고 있어 다른 경로의 세션 수는 1도 안 줄었다. 바뀐 것은 분모(비율 전반 +0.4~4%p)와 1위의 이탈률 A(봇의 "들어와서 바로 나감"이 사람 이탈로 잡히던 5%p가 빠짐)다.
- **중복 제외가 아무것도 안 바꾼 이유**: 세션 기준에서 같은 이벤트 2회는 1이다. 이탈률 A(세션 마지막 이벤트)는 중복 때문에 바뀔 수 있어 확인했는데, 마지막 이벤트가 바뀐 세션 **0**. 봇 제외 표와 봇+중복 제외 표를 diff해 한 글자도 다르지 않음을 확인했다. **이벤트 수 기준이었다면** `product_list_view` 8028→8000으로 달라졌을 값 — "세션 기준으로 세라"는 이유가 여기서 드러난다.
- 강의 퍼널표의 비율(41/62/18/9/4)은 **봇 제외 후 분모(7514)에서만** 재현된다(40.7/62.0/17.4/8.6/3.7). 출제 의도의 필터가 이것이라는 정황이다. 강의의 이탈률 열(44/53/31)은 아래 A·B 어느 정의로도 재현되지 않아 쓰지 않았다.

### 경로 순위 (봇+중복 제외, 분모 = 7514세션)

경로 = 이벤트 이름 단위(묶지 않았다 — 화면과 이벤트가 1:1이고, 묶으면 필터·정렬이 목록에 흡수돼 3위·6위가 사라진다). **이탈률 = A 정의**(그 이벤트가 세션의 마지막이었던 비율 / 그 이벤트가 있는 세션). 실패 = `login_fail`·`client_error`.

| 순위 | 경로 | 세션 수 | 비율 | 이탈률 A | 실패 비용 | E2E 여부(가설) |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `product_list_view` | 7514 | 100.0% | 15.2% | 실패 이벤트 없음. 막히면 **전부** — 모든 세션이 여기서 시작 | ✗ (8주차 E2E 있음) |
| 2 | `product_detail_view` | 4656 | 62.0% | 63.0% | `client_error` 103건 = 상세 세션의 2.2%, 그중 66% 종료. 막히면 담기(17.4%)·찜(6.4%) | ✗ (**화면 없음**) |
| 3 | `category_filter_change` | 3056 | 40.7% | 25.7% | 없음. 막혀도 기본 목록은 됨 | ✗ (8주차 E2E 있음) |
| 4 | `page_change` | 1756 | 23.4% | 37.8% | 없음(전부 2페이지) | ✗ (8주차 E2E 있음) |
| 5 | `cart_add` | 1309 | 17.4% | 50.8% | 없음. 막히면 로그인 시작(8.6%)→주문 전부 — 주문 완료 277세션 **100%가 담기를 거침** | ✗ (8주차 E2E 있음) |
| 6 | `sort_change` | 943 | 12.5% | 28.2% | 없음 | ✗ |
| 7 | `login_start` | 644 | 8.6% | 0.0% | `login_fail` 121건 = 로그인 시작 세션의 18.8%, 그중 48%(58명) 재시도 없이 종료. 막히면 주문서·주문 내역 전부(5.6%·3.7%) → **주문 0건** | **✓ 필수** |
| 8 | `login_success` | 586 | 7.8% | 28.5% | 〃 | (E1에 포함) |
| 9 | `wishlist_add` | 480 | 6.4% | 71.9% | 없음. 로컬 상태 | ✗ (8주차 E2E 경계 케이스에 포함) |
| 10 | `order_start` | 419 | 5.6% | 33.9% | 실패 이벤트 없음. 미완료 34%는 **이탈**로 분류(실패 이벤트가 없어 실패인지 포기인지 구분 불가). 막히면 주문 완료 — 매출이 걸린 유일한 지점 | **✓ 후보** |
| 11 | `order_complete` | 277 | 3.7% | 100.0% | 〃 (종착) | (E4에 포함) |
| 12 | `login_fail` | 121 | 1.6% | 47.9% | 실패 이벤트 자체 | (E3에 포함) |
| 13 | `client_error` | 103 | 1.4% | 66.0% | 실패 이벤트 자체 | ✗ (화면 없음) |

**이탈률 B(퍼널 다음 단계 미도달)** — 퍼널 단계에만 정의된다. 두 정의가 다른 값을 내는 실례: `login_start`는 A=0%(로그인 화면에서 세션이 끝난 사람이 없다 — 전부 성공 또는 실패 이벤트가 따라옴)인데 B=9.0%(성공 못 함). `product_list_view`는 A=15.2% vs B=38.0%(상세 미진입) — 차이 23%p는 필터·정렬만 만지다 떠난 사람이다.

| 단계 | 세션 | 전체 대비 | 앞 단계 대비 | 이탈률 B |
| --- | --- | --- | --- | --- |
| `product_list_view` | 7514 | 100.0% | — | 38.0% |
| `product_detail_view` | 4656 | 62.0% | 62.0% | 71.9% |
| `cart_add` | 1309 | 17.4% | 28.1% | 50.8% |
| `login_start` | 644 | 8.6% | 49.2% | 9.0% |
| `login_success` | 586 | 7.8% | 91.0% | 28.5% |
| `order_start` | 419 | 5.6% | 71.5% | 33.9% |
| `order_complete` | 277 | 3.7% | 66.1% | — |

### 실패 이벤트 — 무엇을 실패로 셌나

`login_fail`과 `client_error` **둘만**. 후보였던 "주문서 진입 후 완료 없음(34%)"은 로그에 실패 이벤트가 없어 실패인지 포기인지 구분할 수 없으므로 실패가 아니라 이탈(A/B)로 분류하고, 실패 비용 열에는 "막히면 뒤따라 막히는 것"만 썼다. "세션 마지막 이벤트"를 실패로 세는 것은 이탈률 정의와 섞이므로 하지 않았다.

## C. 붙일 곳과 안 붙일 곳 (3단계 — 최종 판단)

세 축(빈도 · 실패 비용 · 재현 난이도)으로 판단했다. 세 번째 축 — "통합 테스트로 같은 것을 확인할 수 없는 이유"를 못 대면 E2E로 만들지 않는다.

### 붙이는 것 — 4개 (인증 3 + 로그 근거 1)

| # | 시나리오 | 통합으로 안 되는 것 (E2E인 이유) | storageState |
| --- | --- | --- | --- |
| E1 | 미로그인으로 `/checkout` 진입 → `/login?next=%2Fcheckout` → 로그인 → `/checkout` 복원, 주문서 헤딩 + 헤더에 이름 | proxy의 302, `next` 복원, 세션 쿠키 발급(`httpOnly`·`SameSite`), SSR 헤더 — jsdom에는 서버 렌더·미들웨어·쿠키 속성 해석이 없다. 강의 표의 E2E 세 줄(쿠키·리다이렉트·SSR)이 이 한 테스트에서 같이 확인된다 | **안 씀** — 로그인 자체를 검증한다 |
| E2 | 유효 세션 + `scenario=expired` 쿠키로 `/orders` → "세션이 만료됐어요" + `/login?next=%2Forders` 링크, 헤더는 로그아웃 상태 | 만료 판정이 서버에 있고, 앱 내부 요청에 쿠키가 실리는 경로를 MSW로는 재현하지 못한다(query를 붙일 수 없는 `/api/auth/me`·`/api/orders`) | 씀 |
| E3 | 틀린 비밀번호 → alert "이메일 또는 비밀번호를 확인해주세요." · URL `/login` 유지 · 세션 쿠키 없음 | 강의 기준으로는 **통합 레벨**(MSW 401로 끝). 과제 4단계가 요구해 만들되, E2E 고유 검증은 "실서버 401 + 쿠키 미발급"만이다. **5단계에서 통합 실험과 겹치면 삭제 후보**로 미리 표시한다 | **안 씀** |
| E4 | 로그인 상태에서 `/products` 담기 → 헤더 "주문서" 링크(client navigation) → 주문서에 담은 상품 → 주문하기 → `/orders`에 내역 1건 증가, 헤더 장바구니 0 | 메모리 카트가 **페이지 이동을 건너** 주문서까지 살아 있는가(통합은 한 페이지 안), 주문 후 서버 데이터가 실제로 늘었는가(실서버). 1단계 구현 중 정확히 이 구간에서 prefetch 캐시 리다이렉트 버그가 났다(D1 기록) | 씀 |

E4를 고른 근거(로그): `order_complete`는 3.7%로 11위지만, 담기 세션의 **100%가 주문으로 수렴**하고 주문서에서 이미 34%가 이탈하는 구간이다 — 회귀로 더 나빠져도 "원래 어려운 곳"이라 알아채기 어렵다(강의: 이탈이 몰린 지점). 그리고 매출이 걸린 유일한 지점이다. 빈도가 아니라 실패 비용과 재현 난이도로 고른 것이다.

E1과 E4는 진입 구간이 겹친다(둘 다 주문서에 도착). 합쳐 한 테스트로 만들 수도 있었지만(개수 3 유지) 분리했다 — 5단계에서 인증만 깨뜨렸을 때 실패가 인증 단언에서 멈춰야 진단이 되고, E4는 storageState로 짧게 갈 수 있기 때문이다.

### 빈도 높은데 E2E를 **안 붙인** 경로

`product_list_view`(1위, 100%) · `category_filter_change`(3위, 40.7%) · `page_change`(4위) · `cart_add`(5위, 17.4%). 8주차에 이미 **통합(MSW 응답·URL 반영) + E2E 7개**(`e2e/filters.spec.ts` 4 · `e2e/cart.spec.ts` 3)가 있다. 강의의 "빈도 1위니까 E2E"에 대한 답: 이미 검증한 것을 100배 비싼 도구로 **한 번 더** 붙이는 것이라 추가하지 않는다. 이번 주 E2E는 이 7개 위에 4개를 얹는 것이다.

### 빈도 낮은데 E2E를 **붙인** 경로 — 인증

`login_start` 8.6%, 13개 중 7위. 로그만 보면 후순위다. 붙이는 이유: (1) 깨지면 주문서·주문 내역이 전부 막혀 **주문 0건** — 실패 비용이 최대다. (2) 로그의 `login_fail` 121건 중 58건(48%)은 재시도 없이 떠났다 — 인증이 망가져 전원이 401을 받으면 사용자 눈에는 "내가 틀렸나"로 보여 조용히 떠나고, 로그에는 `login_fail` 급증으로만 남는다. 로그는 이걸 사후에 알려줄 뿐 막아주지 않는다. (3) 미들웨어 302·쿠키 속성·SSR 초기 HTML은 통합 테스트가 볼 수 없다(세 번째 축).

### 로그에 있지만 내 앱에 없는 경로 — 자연 제외

`product_detail_view`(2위, 62%)와 그 뒤의 `client_error`. 상세 화면이 없다. 없는 화면을 만들지 않고, 이 경로가 로그 순위 2위라는 사실과 그럼에도 E2E 대상이 아닌 이유(화면 없음)만 남긴다. 로그 스키마가 구현보다 앞서 정의된 실례다.

### 유지보수 비용 추정 — E1

E1이 지나는 화면: 상품 목록(진입) · 로그인 폼 · 주문서 · 헤더. 각 화면의 문구·구조가 바뀌면:

| 바뀌는 것 | 고칠 곳 |
| --- | --- |
| 로그인 폼 라벨("이메일"·"비밀번호")·버튼("로그인") | `e2e/pom/login.ts` **1파일** — 폼 조작은 함수형 POM(`page`를 받는 `goto`·`fill`·`submit`) 한 곳에만 있다 |
| 헤더 문구("주문서" 링크, "{이름}님") | 헤더 단언 1곳 (E1·E4 각 1줄) |
| 주문서 헤딩("주문서") | 1곳 |
| 라우트 경로(`/checkout`·`/login`) | proxy·page·테스트 3곳 — 이건 앱 변경이지 테스트 취약성이 아니다 |

E1 하나에 **최대 3곳, 폼은 1곳**. 토스인컴 사례("35개 파일 → POM 1개")에서 가져온 기준이고, POM을 클래스가 아니라 함수로 두는 이유는 테스트에서 `page` 흐름이 그대로 읽히기 때문이다.

### 4단계로 넘어가는 것

- 프로젝트 3개: `setup`(워커별 로그인 → `e2e/.auth/worker-N.json`) → `authed`(E2·E4, storageState 사용) / `auth-flow`(E1·E3, storageState **빈 값**).
- 격리: **계정 = 워커**(`looper${parallelIndex+1}`, 8개). 주문은 서버 메모리에 userId별로 쌓이므로 계정 분리가 곧 데이터 분리. 주문 내역 단언은 절대 개수가 아니라 **+1**.
- 셀렉터: 역할·이름 기반. `getByTestId` 없음. 첫 단언은 URL·상태(8주차 리뷰).
- 이 문서만 읽고 4단계에 E1~E4가 들어갈 것과, 필터·카트·상세에 새 E2E가 없을 것을 예측할 수 있어야 한다.

---

## D. 4단계 실행 기록 (E2E 구현)

### 구성

| 파일 | 역할 |
| --- | --- |
| `e2e/base-url.ts` | baseURL 상수. worker fixture는 테스트 스코프 옵션인 `baseURL`을 받을 수 없어 config와 fixture가 이 상수를 함께 쓴다 |
| `e2e/fixtures.ts` | `account`(워커별 계정) · `workerStorageState`(워커별 로그인 파일) worker fixture |
| `e2e/pom/login.ts` · `pom/header.ts` | 함수형 POM. 폼·헤더 문구는 여기에만 있다 |
| `e2e/auth.spec.ts` | E1 · E1-SSR · E3 (storageState **없음**) |
| `e2e/session-expiry.spec.ts` | E2 (storageState 사용) |
| `e2e/order.spec.ts` | E4 (storageState 사용) |

### 시나리오 4개 · 테스트 5개 — 무엇이 늘었나

C절 표의 시나리오는 4개(E1~E4)인데 테스트 파일에는 5개가 있다. **E1을 두 개로 나눴다.**

- `E1` — 미로그인 진입 → 로그인 → 복원 → 주문서 + 헤더 + 쿠키 속성
- `E1-SSR` — 로그인 후 새로 받은 초기 HTML에 로그인 상태가 들어 있는가

C절에서 E1의 "E2E인 이유"에 SSR 초기 HTML을 넣어놨고, 강의 표도 그 세 줄(쿠키·리다이렉트·SSR)이 한 번의 로그인으로 같이 확인된다고 본다. 그런데 SSR 확인은 **JS 실행 전 HTML**을 봐야 해서 `page.request.get()`으로 문서를 다시 받아야 하고, 그걸 E1 끝에 붙이면 "복원됐는가"와 "초기 HTML에 있는가"가 한 테스트에서 죽는다. 5단계 실험 4(초기 상태를 null로)에서 확인했듯이 **분리해 두면 그 실험이 E1-SSR을 직접 지목**한다. 새 시나리오를 추가한 것이 아니라 한 시나리오의 단언을 진단 가능하게 가른 것이다.

### 격리 — 무엇을 갈랐나

**계정**을 갈랐다. `parallelIndex % 8`로 `looper1~8`을 워커에 배분하고(worker fixture, `scope: 'worker'`), 로그인 상태 파일도 `e2e/.auth/worker-<N>.json`으로 워커마다 나눴다. 주문은 서버 메모리에 `userId`별로 쌓이므로 계정을 가르면 데이터도 갈린다 — 워커 4개가 같은 계정으로 주문하면 E4의 "+1" 단언이 서로 밟는다. storageState 파일을 하나로 두면 워커마다 다른 계정으로 로그인해도 파일이 서로 덮어써서 같은 문제가 난다.

**데이터**는 절대값 대신 상대값으로 피했다. E4는 "주문 1건"이 아니라 진입 시점의 개수를 세고 `+1`을 단언한다 — 같은 서버 프로세스에서 이전 실행의 주문이 남아 있어도 결과가 같다.

로그인은 **폼으로** 한다. `request.post('/api/auth/login')`으로 쿠키를 만들면 빠르지만 그건 "로그인 상태를 API로 위조"라 과제가 금지한 것이고, 폼이 깨져도 나머지 테스트가 통과해 버린다.

### storageState의 경계

| 쓴다 | 안 쓴다 |
| --- | --- |
| E2(만료) · E4(주문) — 검증 대상이 로그인이 아니다 | E1 · E1-SSR · E3 — **로그인 자체가 검증 대상**이다 |

`e2e/auth.spec.ts`는 파일 상단에서 `test.use({ storageState: { cookies: [], origins: [] } })`로 비운다. 이미 로그인된 상태로 시작하면 proxy 리다이렉트도, `next` 복원도, 쿠키 발급도 일어나지 않아 **가드를 지워도 통과**한다.

### 셀렉터

전부 역할·이름 기반이고 `getByTestId`는 0건이다. 두 곳에서 좁히기가 필요했다.

- `getByRole('alert')`이 Next의 라우트 안내자(`#__next-route-announcer__`, 역시 `role="alert"`)와 함께 2개로 잡혔다. E3는 `getByRole('main')` 안으로 좁혔고, E2는 error.tsx가 본문을 대체해 `main`이 없으므로 `.filter({ hasText: '세션이 만료됐어요' })`로 문구를 걸었다. 둘 다 테스트 id 없이 해결된다.

### 시간 기반 대기

없다. `waitForTimeout` 0건. 만료는 시계가 아니라 `scenario=expired` 쿠키로 재현한다(TTL을 줄이면 CI 속도에 따라 결과가 달라진다).

### 결과

| 실행 | 결과 |
| --- | --- |
| `--workers=4` | 12 passed (7.2s) |
| `--workers=1` | 12 passed (18.6s) |
| 기본 워커 3회 연속 | 12 passed / 12 passed / 12 passed (6.5 · 6.5 · 6.0s) |

8주차 E2E 7개 + 이번 주 5개(E1 · E1-SSR · E2 · E3 · E4) = 12개. production build 위에서 돈다(`pnpm check`의 build → test:e2e 순서 그대로).

### trace를 열어보고 본 것

E1의 `toHaveURL('/login?next=%2Fcheckout')`을 `%2Fmypage`로 바꿔 실패시키고 `--trace=on`으로 기록해 열었다.

- 타임라인에서 **어디서 멈췄는지가 액션 단위로 보인다**: `Navigate /products`(626ms) → `Click getByRole('banner').getByRole('link', {name:'주문서'})`(34ms) → `Expect toHaveURL`에서 **5006ms** 소모 후 실패. 앞의 두 액션은 정상이었으므로 "가드가 안 돌았다"가 아니라 "기대한 URL이 틀렸다"임이 시간 배분만 봐도 갈린다.
- 대기 로그에 폴링 내역이 그대로 남는다: `unexpected value "http://127.0.0.1:3000/login?next=%2Fcheckout"`가 반복된다 — **실제 값이 무엇이었는지**를 trace가 알려준다. 터미널 출력에도 expected/received가 나오지만, trace에는 그 시점의 DOM 스냅샷(27개)과 네트워크가 함께 있어 "리다이렉트는 됐는데 파라미터만 다르다"를 화면으로 확인할 수 있었다.
- 설정에 `trace` 옵션만 넣어두는 것과 읽는 것은 다르다는 말의 뜻: 실패 원인이 **단언인지 앱인지**를 가르는 게 이 타임라인이다.

이 경험이 8주차 리뷰의 "요소 존재보다 URL·상태를 먼저 단언" 지적과 맞물린다. 이번 주 5개 테스트는 전부 `goto`/`click` 직후 URL을 먼저 단언하고 요소는 그 뒤에 본다.

---

## E. 5단계 자가 검증 — E2E가 무엇을 잡는지

실험마다 **인증 구현 한 곳만** 바꾸고 `pnpm test`(단위·통합 146개)와 `pnpm test:e2e`(12개)를 둘 다 돌렸다. 테스트 코드는 건드리지 않았다. 매 실험 뒤 `git checkout -- src/`로 되돌렸다.

| # | 망가뜨린 곳 | 어떻게 바꿨나 | 결과 | 실패한 테스트 | 실패 메시지로 원인을 알 수 있었나 |
| --- | --- | --- | --- | --- | --- |
| 1 | `src/proxy.ts` 가드 조건 | `has(SESSION_COOKIE)` → `!has(...)` (로그인한 사람만 튕김) | 잡힘 | E1 · E2 · E4 (3개) | ✅ `toHaveURL` 실패. 다만 3개가 동시에 죽어 "가드 자체가 뒤집혔다"는 건 세 실패의 공통점을 봐야 안다 |
| 2 | `src/proxy.ts` 복원 경로 | `searchParams.set('next', …)` 삭제 | 잡힘 | E1 (1개) | ✅ expected `/login?next=%2Fcheckout` / received `/login` — 파라미터만 없다는 게 바로 보인다 |
| 3 | `entities/order/api/order.api.ts` 만료 분기 | 401 → `UnauthorizedError` 승격 제거(HttpError로 떨어짐) | 잡힘 | E2 (1개) | ✅ "세션이 만료됐어요"를 못 찾음. 화면이 일반 오류 문구로 바뀐 것을 문구로 지목 |
| 4 | `app/(commerce)/layout.tsx` 초기 상태 | 서버가 읽은 user 대신 `null`을 캐시에 넣음 | 잡힘 | E1 · E1-SSR · E4 (3개) | ✅ E1-SSR이 "초기 HTML에 이름 없음"을 직접 짚는다. E1·E4는 헤더 단언에서 죽어 같은 원인을 두 번 가리킨다 |
| 5 | `api/auth/login/route.ts` 쿠키 속성 | `httpOnly: true` → `false` | 잡힘 | **단위**(스타터 `route.test.ts`) + E1 | ✅ 둘 다 잡음 — 아래 재판단 참조 |
| 6 | `features/login/ui/LoginForm.tsx` | 로그인 성공 후 `router.refresh()` 삭제 | **살아남음** | 없음 (12개 전부 통과) | — |
| 7 | `app/SiteHeader.tsx` | 보호 경로 링크의 `prefetch={false}` 제거 | 잡힘 | E1 (1개) | ✅ 로그인 후 URL이 `/login?next=%2Fcheckout`에 머문다 — 캐시된 리다이렉트를 탄 것 |

### 통합 테스트로는 잡을 수 없었던 것

**실험 1 · 2 · 4 · 7이 그렇다.** 넷 다 vitest 146개가 전부 통과했다.

- 1 · 2 — 미들웨어(proxy)는 서버에서만 돈다. jsdom에는 302도, 그 뒤 브라우저가 따라가는 과정도 없다.
- 4 — 초기 HTML의 로그인 상태. jsdom에 서버 렌더 단계가 없어 "JS 실행 전 화면"을 만들 수 없다.
- 7 — Next 라우터 캐시. 앱 안의 페이지 이동이 실제로 일어나야 재현된다.

실험 3은 통합으로도 잡을 수 있는 종류다(MSW로 401을 주고 경계 문구를 보면 된다). 다만 그 통합 테스트를 아직 쓰지 않았기 때문에 지금은 E2E만 잡았다.

### 실험 5 재판단 — E3를 남길 것인가

실험 5는 **스타터의 단위 테스트가 먼저 잡았다.** 3단계 C절에서 "E3(잘못된 자격 증명)는 강의 기준 통합 레벨이고, 5단계에서 겹치면 삭제 후보"라고 미리 적어둔 그 자리다.

**남기기로 했다.** 근거: 단위 테스트가 보는 것은 `Set-Cookie` **헤더 문자열**에 `HttpOnly`가 있는지이고, E1이 보는 것은 **브라우저 쿠키 저장소**에 담긴 속성이다. 헤더를 맞게 써도 `Secure` 조합·도메인·경로 때문에 브라우저가 다르게 저장하는 경우가 있어 확인 주체가 다르다(강의: "쿠키를 굽는 건 서버, 해석하는 건 브라우저"). 다만 **E3 자체**(틀린 비밀번호 → 문구 + 쿠키 없음)는 실험 5로는 재판단되지 않았다 — E3를 죽이는 실험은 따로 하지 않았고, E3가 지키는 것은 "실서버 401에서 쿠키가 아예 발급되지 않는다"이다. 다음 주에 통합 테스트로 로그인 실패 화면을 덮으면 그때 다시 판단한다.

### 살아남은 실험 6 — 무엇을 했나

`router.refresh()`를 지웠는데 12개가 전부 통과했다. 테스트를 늘리기 전에 **왜 안 잡혔는지**부터 봤다.

이 줄은 1단계 구현 중 만난 버그(로그인 후 `router.replace('/checkout')`가 캐시된 리다이렉트를 타고 로그인 화면으로 되돌아옴)의 수정이었다. 그런데 같은 버그에 수정이 **두 개** 들어가 있었다 — `prefetch={false}`(헤더 링크)와 `router.refresh()`(로그인 성공 후). 실험 7이 그중 무엇이 실제로 버그를 막고 있는지 갈라 준다: `prefetch={false}`를 빼면 E1이 죽고, `router.refresh()`를 빼면 아무 일도 없다.

**대응: 테스트를 추가하지 않고 `router.refresh()`를 지웠다.** 이유는 두 가지다.

1. 이 줄이 막는 상황을 만들려면 prefetch가 켜져 있어야 하는데, 그건 실험 7이 이미 "그러면 안 된다"고 확인한 설정이다. 즉 지금 코드에서는 도달할 수 없는 방어다.
2. 새 E2E 시나리오를 만드는 것은 3단계 표에 없는 테스트를 늘리는 것이라 이번 주 규칙에 어긋난다. 근거 없이 개수를 늘리지 않기로 한 것이 이 과제의 핵심이다.

지운 뒤 12개 전부 통과하고, 실험 7을 다시 돌리면 여전히 E1이 잡는다 — **진짜 방어선은 `prefetch={false}` 하나**임을 확인했다. 살아남은 변형이 알려준 것은 테스트의 구멍이 아니라 **구현의 중복**이었다.

### 손으로 고른 실험의 한계

실험 1~5는 "내가 깨질 것 같다고 생각한 곳"이고 전부 잡혔다. 6·7은 **내가 오늘 실제로 만난 버그의 수정 지점**을 겨냥해 추가한 것인데, 살아남은 하나가 여기서 나왔다. 8주차 리뷰에서 지적받은 "손으로 고르는 방식의 사각"이 그대로 재현된 셈이다 — 사각은 "생각 못 한 코드"가 아니라 **"이미 고쳤으니 맞겠지" 하고 다시 안 본 코드**에 있었다.

---

## F. 코드 리뷰 반영 (제출 직전)

작성한 코드를 다시 훑어 5건을 고쳤다. 그중 하나는 2단계의 주장 자체를 무너뜨리는 것이었다.

### 1. 첫 이벤트에 공통 프로퍼티가 안 붙었다 (`app/AnalyticsProvider.tsx`)

`track()`은 **호출 시점**에 공통 프로퍼티를 합쳐 큐에 넣는다(로거 계약, `logger.ts`의 `properties: { ...commonProperties(), ...properties }`). 그런데 `setCommonProperties()`를 `AnalyticsProvider`의 effect에서 불렀다. effect는 자식이 먼저 도므로, 화면의 계측 effect가 등록보다 앞서면 그 이벤트에는 공통 프로퍼티가 **영영** 붙지 않는다. 큐는 순서를 지킬 뿐 이미 합쳐진 props를 고쳐주지 않는다.

실제로 재현됐다. `/login?next=%2Fcheckout` 첫 진입의 `login_start`가 `from` 하나만 달고 나갔다 — `sessionId`도 `device`도 `ts`도 없이. 3단계 B절의 집계가 `sessionId`로 세션을 묶는데, 로그인 시작 이벤트에 그게 없으면 퍼널이 끊긴다.

**A절에 "초기화 전 이벤트는 큐가 잡는다"고 쓴 것이 반만 맞았다.** 큐가 잡는 것은 전송 시점이지 프로퍼티가 아니다.

`/products`는 정상이었다. `ProductsPage`가 `<Suspense>` 안에 있어 하이드레이션이 늦어지고, 그 덕에 우연히 등록이 먼저 끝났기 때문이다. **타이밍에 기대고 있었다는 뜻이라 통과한 쪽이 더 나쁜 신호였다** — 처음 확인할 때 `/products`만 봐서 놓쳤다.

고침: `registerProviders`·`setCommonProperties`를 모듈 스코프로 옮겼다. 두 함수는 모듈 변수에 값만 넣으므로 서버 평가에서도 안전하고, 어떤 컴포넌트 effect보다 먼저 실행된다. `initAnalytics()`만 effect에 남겼다(브라우저에서 한 번). 재검증: `/login` 첫 진입의 `login_start`에 `sessionId,device,ts,from`이 전부 붙는다.

### 2. 렌더 중 캐시 쓰기 (`(commerce)/error.tsx`)

만료 화면에서 `queryClient.setQueryData(...)`를 렌더 본문에서 불렀다. 렌더 도중에 다른 컴포넌트(헤더)의 구독을 깨우는 것이라 React가 경고하는 패턴이고, 재렌더마다 다시 쓴다. `useEffect`로 옮겼다.

### 3. 로딩 중에 개수를 세고 있었다 (`e2e/order.spec.ts`)

E4가 진입 시점의 주문 수를 `.count()`로 재는데, `count()`는 재시도하지 않는다. `/api/orders`가 500ms 지연되므로 그 순간 화면은 아직 로딩이고 개수는 0이다. 헤딩 단언은 로딩 중에도 통과하니 걸러지지 않았다.

지금까지 통과한 것은 매 실행이 새 서버(빈 메모리)로 시작해 실제 개수도 0이었기 때문이다. 서버를 재사용하거나 재시도가 걸리면 실제 N개인데 0을 읽고 `+1`을 기대해 실패한다. **"절대 개수 대신 상대값"이라고 적어둔 의도가 코드에서는 지켜지지 않고 있었다.** 로딩 표시가 사라진 뒤에 세도록 고쳤다.

### 4. 복원 경로에서 쿼리스트링이 사라졌다 (`(commerce)/error.tsx`)

만료 화면의 "다시 로그인" 링크를 `usePathname()`만으로 만들어서 `/orders?page=2`에서 만료되면 `/login?next=%2Forders`가 됐다. proxy는 `pathname + search`를 싣는데(D4) 두 진입점이 서로 다른 곳으로 돌려보내고 있었다. `useSearchParams()`를 함께 써서 맞췄다. 재검증: `/login?next=%2Forders%3Fpage%3D2`.

### 5. 집계 기간을 파일 순서로 구했다 (`scripts/analyze-events.mts`)

`events.at(0)`·`events.at(-1)`의 `ts`를 기간으로 출력했는데, 이 값은 파일 순서일 뿐 시간순이 아니다(세션이 섞여 들어온다). B절에 인용한 숫자라 `ts`의 최소·최대로 바꿨다. 결과는 같았지만(2026-07-22 ~ 2026-08-20) 우연이었다.

### 남는 것

1·3은 **"확인했다"고 적은 것이 실제로는 한 경로만 본 것**이었다. 1은 `/products`만 보고 전체를 판단했고, 3은 새 서버에서만 돌려보고 "상대값이라 안전하다"고 적었다. 다음 주차에는 "이 확인이 통과하는 다른 이유가 있는가"를 한 번 더 묻겠다.

---

## G. D2 정정 — proxy는 서명까지 검증한다 (제출 직전)

동료의 9주차 PR에서 proxy가 `readSessionToken`으로 서명과 만료를 확인한다는 대목을 보고, 내 D2 근거("Edge 런타임이라 `node:crypto`를 못 쓴다")가 맞는지 실측했다.

**틀렸다.** Next 16.2.10의 proxy는 Node 런타임에서 돈다. `src/proxy.ts`에서 `auth.ts`의 `readSessionToken`을 그대로 import해도 `pnpm build`가 통과하고, `pnpm start`에서도 정상 동작한다.

| 요청 | 결과 |
| --- | --- |
| 미로그인 `/orders` | 307 → `/login?next=%2Forders` |
| 정상 로그인 쿠키 `/orders` | 200 |
| 위조 쿠키 `session=fake.sig` `/orders` | 307 → `/login?next=%2Forders` |
| 서버 로그의 crypto·Edge 오류 | 없음 |

스타터 `auth-cookies.ts`의 주석("Edge 런타임에서 도는 코드(proxy · middleware)가 이 값들을 가져갈 수 있어야 하므로")을 **검증 없이 제약으로 받아들인 것**이 원인이다. 그 주석은 파일을 나눠 둔 이유를 설명한 것이지, 이 버전의 proxy 런타임을 단정한 것이 아니었다. 나는 `pnpm start`로 302를 확인하는 데까지는 갔지만, "서명 검증을 넣으면 실제로 깨지는가"는 시도조차 하지 않았다.

**고쳤다.** proxy가 쿠키 존재 대신 `readSessionToken`의 결과를 본다. 얻는 것:

- 위조·만료 쿠키가 보호 화면까지 들어오지 못한다. 전에는 401 경계까지 가서야 튕겼다.
- "로그인했는가"의 판정이 proxy와 `(commerce)` 레이아웃에서 같은 함수로 통일된다.
- D5의 만료 판정 기준은 그대로 성립한다. proxy가 통과시킨 요청은 그 시점에 유효한 세션이었으므로, 화면까지 온 401은 그 뒤에 만료됐거나 서버가 세션을 무효화한 경우다. 기준의 근거가 "쿠키가 있었다"에서 "유효한 세션이었다"로 오히려 단단해졌다.

`pnpm check` 전체 통과(146 + 12). 5단계 실험 1(가드 조건 반전)은 이 구현에서도 같은 세 테스트가 잡는다.

이번 주에 같은 실수를 세 번 했다. F절의 두 건은 "확인했다"가 한 경로만 본 것이었고, 이 건은 **남이 써 둔 주석을 확인 없이 제약으로 받아들인 것**이다. 셋 다 통과한 결과를 다시 의심하지 않아서 생겼다.

---

## H. Advanced A — 시각적 회귀 (Basic 제출 후)

### 갤러리를 어디에 뒀나 — Next 라우트, CT 러너 없이

과제가 말하는 gallery는 "`window.mount()`를 노출해 story를 그려주는 페이지"다. Playwright의 실험적 컴포넌트 테스트(`@playwright/experimental-ct-react`)는 Vite로 따로 번들하므로 `next/image`·`next/font`·CSS 모듈이 실제와 다르게 올라온다. 갤러리를 앱 안의 라우트(`src/app/gallery`, 정적 ○)로 두면 8주차부터 쓰던 production build 위 Playwright를 그대로 쓰고, 기존 `pnpm test:e2e`에 spec 하나가 얹힌다. `dialog-demo`·`select-demo`가 이미 이 방식이다.

- story = props를 고정한 래퍼(`ProductsSkeleton.story.tsx`의 named export). 인자를 받지 않는다 — 받으면 호출부마다 그림이 달라져 기준선이 무엇의 기준선인지 흐려진다.
- 레지스트리(`gallery/stories.ts`)의 키가 story 이름이고, POM(`e2e/pom/gallery.ts`)은 그 키 타입으로만 받는다. 이름 오타는 typecheck에서 걸린다.
- `window.mount`는 한 번에 story 하나만 그린다. 여러 개를 늘어놓으면 하나가 바뀔 때 전부의 기준선이 깨지고, 뷰포트 밖 story는 이미지 지연 로딩 때문에 안 뜬 채로 찍힌다. 없는 이름은 `throw` — 빈 화면이 기준선으로 통과하는 것이 스크린샷 테스트의 가장 흔한 거짓 통과다.
- 크롬(헤더)은 빼고 `week05-page` 컨테이너만 두른다. 폭 1200px이 5열 그리드의 칸 너비를 정하므로 이게 없으면 기준선이 실제 화면과 어긋난다.

첫 대상은 `ProductsSkeleton`. 폰트·이미지·시간이 없는 가장 결정적인 컴포넌트라 파이프라인 자체를 검증하기 좋다. story 2개: 기본(12개)과 **덜 찬 줄(3개)**. 후자를 둔 이유는 아래 실험 2다.

### 비결정성 — 확인했더니 대부분 소스가 없었다

| 항목 | 이 앱에서 | 처리 |
| --- | --- | --- |
| 애니메이션 | `globals.css`·`week-05-layout.css`에 `animation`·`transition`·`@keyframes` 0건 | `toHaveScreenshot` 기본값(`animations: 'disabled'`, `caret: 'hide'`) 유지 |
| 폰트 | `next/font/google`이 빌드 시점에 self-host — 런타임 네트워크 없음. 스켈레톤은 글자 자체가 없음 | 없음 |
| 이미지 | 스켈레톤엔 없음. `ProductCard`의 `<Image>`는 로컬(`/images/products/*.jpg`)이지만 `priority` 없이 lazy | 이 story에선 해당 없음. 카드 story를 만들 때 뷰포트 안에서 하나만 그리는 것으로 대응 |
| 시간·랜덤 | 없음 | 없음 |
| 뷰포트 | `week05-grid`가 960/720px에서 열 수가 바뀜 | config의 Desktop Chrome(1280×720) 고정. 요소 스크린샷이라 높이는 무관(기본 story는 1200×928로 뷰포트 밖까지 찍힘) |

**마스킹은 하지 않았다.** 마스킹은 매번 달라지는 영역(시계·광고·랜덤 추천)을 가리는 것인데 이 story엔 그런 영역이 없다. 대상이 없는데 가리면 "왜 가렸는지"를 설명할 수 없다.

### 기준선 — 커밋한다

- `pnpm check`에 `test:e2e`가 들어 있으므로, 기준선이 레포에 없으면 새 머신의 첫 `pnpm check`는 무조건 실패한다(기준선을 쓰고 실패하는 Playwright 동작). 기준선이 곧 "이 커밋이 기대하는 화면"이라 코드와 같이 버전 관리되는 게 맞다.
- 파일명에 `-chromium-darwin`이 붙는다. Linux CI는 `-chromium-linux.png`를 찾으므로 **렌더링이 달라서가 아니라 파일이 없어서** 실패한다. CI를 붙일 때는 Playwright 도커 이미지 안에서 Linux 기준선을 따로 만들어 함께 커밋하거나, 시각 테스트를 CI에서 제외해야 한다. 지금은 CI가 없어 결정을 미룬다.
- 크기: 2장 합쳐 7.5KB. 카드 story가 늘어나 이미지가 들어가면 다시 본다.

### 실험 — 의도한 변경 1회 · 의도하지 않은 변경 1회

스펙과 기준선은 건드리지 않고 CSS만 바꿨다. 매 실험 `pnpm build` 후 실행(E2E는 production build 위에서 돈다).

| # | 종류 | 바꾼 것 | 결과 |
| --- | --- | --- | --- |
| 1 | 의도한 변경 | `ProductsSkeleton.module.css` 회색 `#ececec` → `#e3e3e3` (3곳) | **안 잡힘** — 2개 전부 통과 |
| 1′ | 같은 변경, `threshold: 0` | (위와 같음) | 잡힘 — 기본 682,368px(62%) · 덜 찬 줄 171,564px(47%) 다름 |
| 2 | 의도하지 않은 변경 | `week-05-layout.css` `repeat(5, minmax(0, 1fr))` → `repeat(auto-fit, minmax(200px, 1fr))` | 잡힘 — **덜 찬 줄만** 실패(308px → 471px 높이, 322,276px·58% 다름). 기본(12개)은 통과 |

**실험 1이 통과해 버린 이유.** `toHaveScreenshot`의 기본 `threshold`는 0.2이고, 이건 픽셀 개수 비율이 아니라 **픽셀 하나의 YIQ 색 거리 허용치**다. 회색 9/255 차이는 그 안에 들어간다. 계산해 보면 기본값에서 회색이 "다르다"고 잡히려면 약 53/255(20% 이상) 움직여야 한다 — 디자인 토큰 한 단계 조정은 전부 통과한다는 뜻이다. 실험 결과를 확인한 뒤 빌드 CSS에 `#e3e3e3`가 실제로 들어갔는지(`.next/static/chunks`) 먼저 봤다 — 스테일 빌드가 아니었다.

`threshold: 0`으로 낮추자 잡혔고, 그 상태에서 `--update-snapshots`로 기준선을 갱신하면 통과한다(의도한 변경의 정상 절차). 실험 뒤 기준선은 원래 것으로 되돌렸다. **허용치 0을 config에 남긴다** — 이 앱은 애니메이션·폰트 로딩이 없어 같은 트리는 픽셀까지 같고(원복 후 3회 연속 통과), 허용치를 두는 유일한 이유였던 "흔들림"이 없다. 흔들리는 story가 생기면 그 story에서만 올린다.

**실험 2가 덜 찬 줄에서만 잡힌 이유.** 1200px에서 `auto-fit, minmax(200px, 1fr)`은 (1200+20)/(200+20)=5열이라 가득 찬 격자는 픽셀이 같다. 빈 트랙이 접히는 건 줄이 덜 찼을 때뿐이다 — 카드 3개가 386px로 늘어났다. "5열 고정"이라는 계약의 회귀는 **가득 찬 화면에선 보이지 않는다.** 이 story를 처음부터 둔 근거가 그것이고, 실험이 그대로 확인했다. 같은 변경을 8주차 통합 테스트(jsdom)는 잡을 수 없다 — 레이아웃 계산이 없다.

### 남는 것

- 이미지가 있는 story(`ProductCard`)는 아직 없다. lazy 로딩·`srcset` 선택이 붙는 순간 비결정성 표의 "해당 없음" 세 칸이 다시 열린다.
- `threshold: 0`은 이 앱의 조건(애니메이션·폰트 로딩 없음)에 기댄 값이다. 그 조건이 깨지면 값이 아니라 조건을 먼저 본다.
